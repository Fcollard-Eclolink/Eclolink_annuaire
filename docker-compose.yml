services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./app:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
      - NUXT_HOST=0.0.0.0
      - NUXT_PORT=3000
      - NUXT_SUPABASE_URL=${SUPABASE_URL}
      - NUXT_SUPABASE_KEY=${SUPABASE_KEY}
